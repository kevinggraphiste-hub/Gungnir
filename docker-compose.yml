services:
  app:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./backend/plugins:/app/backend/plugins
      - ./backend/data:/app/backend/data
